CUBIXIA 1.0.20 Gmail Setup

The sender Gmail is now built in:

  gglitch145@gmail.com

Render still needs this Environment Variable:

  GMAIL_APP_PASSWORD = kebp texa yqyw eyaz

Do not use your normal Gmail password.

After adding it in Render:

  Save Changes
  Manual Deploy
  Clear build cache & deploy

Then check:

  https://cubixia.onrender.com/health

You want:

  "version": "1.0.20"
  "gmailUserSet": true
  "gmailPasswordSet": true
  "gmailReady": true

If gmailPasswordSet is false, the Render service still does not have GMAIL_APP_PASSWORD.
